import React from 'react';
import "./NoRoom.css";

//when user tried to redirect to a room that don't exist
const NoRoom = () => {
    
    return (
    	<div>
            Error
        </div>
    );
}

export default NoRoom;